<div class="container">
  <h2>Add bookmark for movie: <%= @movie.title  %></h2>
  <%= simple_form_for([@movie, @bookmark]) do |f| %>
  <%= f.input :comment %>
  <%= f.association :list%>
  <%= f.submit %>
<% end %>
</div>
