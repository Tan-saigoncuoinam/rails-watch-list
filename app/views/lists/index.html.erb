<div class="container">
  <h1>Lists(movies) to watch</h1>
  <ul>
    <% @lists.each do |i| %>
      <li>
        <%= i.name %>
        <%= link_to i.name, list_path(i) %>
      </li>
    <% end %>
  </ul>
  <p><%= link_to "add new list", new_list_path, class: "btn btn-primary"  %></p>
</div>
